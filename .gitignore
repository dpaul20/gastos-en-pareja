# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report/
/test-results/
e2e/auth.json

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.zip
"Gastos en Pareja Design System*"

# local design system / AI-generated scratch work
/docs/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# GGA (Gentleman Guardian Angel) AI code review config
.gga

# Claude Code / AI agent artifacts
# settings.json IS tracked — it is the shared project config. Only the
# .local override is per-machine: absolute paths, tool-result UUIDs, and a
# permission allowlist that means nothing on anyone else's box.
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock
.playwright-mcp/
*.worktree

# Agent Teams Lite skill registry cache (local, regenerated per machine)
.atl/

# Audit screenshots dumped in root
/*.png

# Ad-hoc scripts and test reports
/qacito_tests/
/scripts/

# Serwist generated service worker
/public/sw.js

# Design system docs (generated, not versioned)
/docs/gastos-en-pareja-design-system/
